#pragma once

#include "pch.hpp"

#include "Error.h"

//[[nodiscard]] b::expected<std::nullopt_t,Error> cli_new(const ProjectData& project);
//[[nodiscard]] b::expected<std::nullopt_t,Error> cli_configure(const ProjectData& project);
//[[nodiscard]] b::expected<std::nullopt_t,Error> cli_build(const ProjectData& project);
//[[nodiscard]] b::expected<std::nullopt_t,Error> cli_run(const ProjectData& project);
//
//[[nodiscard]] b::expected<std::nullopt_t,Error> cmake_configure(const ProjectData& project);
//[[nodiscard]] b::expected<std::nullopt_t,Error> cmake_build(const ProjectData& project);
//[[nodiscard]] b::expected<std::nullopt_t,Error> cmake_run(const ProjectData& project);

/*template<typename T>
std::string rangToString(T color) {
    rang::setControlMode(rang::control::Force);
    rang::setWinTermMode(rang::winTerm::Ansi);

    std::stringstream ss;
    ss << color;

    rang::setControlMode(rang::control::Auto);
    rang::setWinTermMode(rang::winTerm::Auto);

    return ss.str();
}*/
