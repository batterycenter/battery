
#include "battery/core/all.hpp"
#include <gtest/gtest.h>

TEST(BatteryCore_TrayIcon, MultipleTimes) {
    //auto& tray = battery::tray_icon::get();
    //for (int i = 0; i < 5; i++) {
    //    EXPECT_EQ(tray.create(), true);
    //    battery::sleep(0.2);
    //    tray.destroy();
    //    battery::sleep(0.2);
    //}
}
